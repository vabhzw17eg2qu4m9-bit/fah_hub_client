name: fah_hub_client
description: >-
  DAP/1 hub client for flutter_agent_harness: MessagingRepository over a
  signed WebSocket, FahPlugin wiring delivery via Agent.externalSteeringSource.
version: 0.2.8
repository: https://github.com/IstiN/fah_hub_client

environment:
  sdk: ^3.0.0

dependencies:
  cryptography: ^2.7.0

dev_dependencies:
  coverage: ^1.15.1
  test: ^1.31.0
